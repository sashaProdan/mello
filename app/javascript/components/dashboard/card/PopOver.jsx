import React from 'react';
import PropTypes from 'prop-types';

class PopOver extends React.Component {

  render() {
    return {
      <div class={`popover ${this.props.type}`}>

      </div>
    }
  }
}

export default PopOver;